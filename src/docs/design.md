# LLM

- `history` belongs with the LLM client > the REPL since
  executing code directly can have multiple calls i.e asking an
  LLM to explore a codebase can trigger tool calls which the LLM
  responds to etc.
